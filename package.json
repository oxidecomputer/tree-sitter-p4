{
  "name": "tree-sitter-p4",
  "version": "0.0.1",
  "description": "Tree-sitter P4 grammar",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oxidecomputer/tree-sitter-p4.git"
  },
  "main": "index.js",
  "types": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp-build",
    "prebuildify": "prebuildify --napi --strip",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "Ryan Goodfellow",
  "license": "MPL-2.0",
  "dependencies": {
    "node-addon-api": "^7.1.0",
    "node-gyp-build": "^4.8.0"
  },
  "peerDependencies": {
    "tree-sitter": "^0.25.0"
  },
  "peerDependenciesMeta": {
    "tree_sitter": {
      "optional": true
    }
  },
  "devDependencies": {
    "prebuildify": "^6.0.0",
    "prettier": "^3.2.4"
  },
  "files": [
    "grammar.js",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**"
  ]
}
